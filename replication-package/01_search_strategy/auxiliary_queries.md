# Auxiliary Queries

The manuscript explicitly reports the following post-quantum auxiliary query:

```text
"post-quantum" AND "blockchain"
```

It also states that auxiliary searches addressed formal verification and DeFi-specific attacks, but the exact strings, execution dates, databases, and result counts are not present in the supplied artifacts. They are therefore marked as unavailable rather than reconstructed.
