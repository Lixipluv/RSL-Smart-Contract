# Canonical Search String

Derived from the PICOC protocol and reported in Section 2.4 of the manuscript.

```text
("smart contract" OR "decentralized application" OR "Web3")
AND ("security" OR "vulnerability")
AND ("artificial intelligence" OR "machine learning"
     OR "deep learning" OR "graph neural network"
     OR "large language model")
AND ("detection" OR "analysis" OR "audit")
```

**Publication window:** 2020--2026, with foundational references allowed regardless of period.

**Languages:** English or Portuguese.

**Primary sources:** IEEE Xplore, ACM Digital Library, Springer Link.

**Important provenance note:** The manuscript states that the canonical query was adapted to each database. The exact executed database-specific strings and execution dates are not present in the supplied manuscript/export, so they are not reconstructed here. They should be replaced only from the original Parsifal/database history.
